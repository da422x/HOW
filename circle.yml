machine:
  node:
    version: 6.8.1

test:
  override:
    - grunt test --force

dependencies:
  # pre:
  #   - npm install -g npm
  # post:
  #   - grunt build

general:
  only: ['master', 'nitenite']

deployment:
  production:
    branch: master
    commands:
      - grunt build
      - npm run surge
  nightly:
    branch: nitenite
    commands:
      - grunt build
      - npm run surge