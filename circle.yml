test:
  override:
    - grunt test --force

dependencies:
  pre:
    - npm install -g npm

deployment:
  production:
    branch: master
    commands:
      - ./node_modules/surge/lib/cli.js --project ./docs