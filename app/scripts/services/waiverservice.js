'use strict';

/**
 * @ngdoc service
 * @name ohanaApp.waiverService
 * @description
 * # waiverService
 * Service in the ohanaApp.
 */
angular.module('ohanaApp').service('waiverService', function() {
  // AngularJS will instantiate a singleton by calling "new" on this function
});
